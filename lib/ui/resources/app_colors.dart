import 'package:flutter/material.dart';

const Color lightPrimary      = Colors.white;
const Color darkPrimary       = Colors.black;
const Color darkAccent        = Color(0xff00363a);
const Color lightAccent       = Color(0xff428e92);
const Color intermedioColor   = Color(0xff006064);
const Color lightBG           = Colors.white;
const Color darkBG            = Color(0xff070709);
const Color secondBadgeColor  = Color(0xFF009faf);
const Color badgeColor        = Color(0xff88ffff);
const Color buttonColor       = Color(0xff4dd0e1);
const Color transparentColor  = Colors.transparent;
const Color opacityTitleColor = Color(0x80657291);
const Color greyBG            = Color(0xFFbbb6c2);
const Color lightGreyColor    = Color(0xFFBEBEBE);

// Acá Ale tiene que ir lo que utilices con respecto a colores la parte de arriba es la general,
// si necesitas algo en personal para una sola cosa, lo pones debajo de esto
// escribiendo un comentario y explicando el porqué :D

// Ej. Estos colores pertencen a VerbosPage para el uso de tal cosa
