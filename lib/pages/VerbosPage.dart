import 'package:flutter/material.dart';

class VerbosPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("VerbosPage"),
    );
  }
}